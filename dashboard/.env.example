# Server-side only — never exposed to the browser (no NEXT_PUBLIC_ prefix).
SUPABASE_URL=
SUPABASE_SERVICE_KEY=

# Password to unlock the dashboard. Pick something strong — this gates
# pause/resume and trade-size controls. Required; the dashboard refuses to
# serve any page without it set.
DASHBOARD_PASSWORD=
